// import fs from "fs/promises";
// import path from "path";
// import matter from "gray-matter";
// import showdown from "showdown";

export default async function singleMarkdown(id) {
  console.log("single Markdown js")
  const ID = id;

  return parseInt(ID);
}
